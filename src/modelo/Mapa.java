package modelo;

/**
 * 
 */
public class Mapa extends Objeto {
    /**
     * Default constructor
     * @param x 
     * @param y 
     * @param ancho 
     * @param alto
     */
    public Mapa(float x, float y, float ancho, float alto) {
        super(x, y, ancho, alto);
    }
}